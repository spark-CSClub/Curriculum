public class Practice
{
	/**
	 * This question mainly measures your ability in Loop Structure.
	 * given that an integer is the product of two prime
	 * numbers. Please return the larger prime.
	 *
	 * @param n the given integer
	 * @return the larger prime number
	 */
	public static int largerPrime(int n)
	{
		//your code here
		return -1;
	}

	/**
	 * This question mainly measures your ability in Sequence Structure.
	 * reverse a three digit number. For example, if input n is
	 * 102, you are supposed to return 201. if there are any zero
	 * at the beginning of the reversed number, omit them. for
	 * example, if input n is 100, you should return 1
	 * Remember that you are not allowed to use any methods related
	 * to String.
	 *
	 * @param n a three digit number
	 * @return reversed number
	 */
	public static int reverseThreeDigitNumber(int n)
	{
		//your code here
		return -1;
	}

	/**
	 * This question mainly measures your ability in Control Structure.
	 * Suppose that you are going to post a package. From the post
	 * office, you know the following information:
	 * 1. for package whose weight is within 1000 grams (including 1000
	 * grams), the basic fee is $8.
	 * 2. for package more than 1000 grams, the extra charge is $4 per
	 * 500 grams(calculate as 500 grams if the extra part is less than
	 * 500 grams).
	 * 3. If the user chooses to post urgently, charge $5 more.
	 * calculate the fee you need to pay with the given weight.
	 * @param weight the weight of the package
	 * @param isUrgent whether the package is urgent. True for urgent
	 *                 while false for not urgent.
	 * @return the money you should pay to post the package
	 */
	public static int postage(int weight, boolean isUrgent)
	{
		//your code here
		return -1;
	}
}
